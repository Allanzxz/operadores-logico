/* 
11. Explique a diferença entre os operadores && e || em JavaScript. 
*/
//resposta: true && true retorna true, porque ambos os operandos são verdadeiros.
//true || false retorna true, porque pelo menos um dos operandos é verdadeiro.